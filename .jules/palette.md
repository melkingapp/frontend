## 2024-07-01 - Add role="alert" to dynamically rendered error messages
**Learning:** For accessible form validation in UI components, always set role="alert" on the dynamically rendered error message container (e.g., <p role="alert">) so screen readers announce the error immediately upon appearance.
**Action:** Always add role="alert" to inline error messages in input components.
