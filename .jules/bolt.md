## 2024-07-29 - Redux Selector Fallbacks
**Learning:** Returning inline arrays (e.g., || []) from useSelector breaks referential equality and causes unnecessary re-renders.
**Action:** Define stable constants outside components for fallbacks.
